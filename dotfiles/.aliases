alias vli="LC_COLLATE=C ls -AlFhp --color"
alias li="LC_COLLATE=C ls -lFhp --color --group-directories-first"
alias mkdir="mkdir -p"
alias e="$EDITOR"
alias v="$VISUAL"

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'
